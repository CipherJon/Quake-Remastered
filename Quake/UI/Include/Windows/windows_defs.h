#pragma once

#include <windows.h>

// Type aliases for common Windows types to improve code readability
typedef PIXELFORMATDESCRIPTOR PixelFormatDescriptor; // Alias for pixel format descriptor
typedef WNDCLASS WndClass;                          // Alias for window class
typedef HWND Hwnd;                                  // Alias for window handle
typedef DWORD DWord;                                // Alias for double word
typedef MSG Msg;                                    // Alias for message
typedef LRESULT LResult;                            // Alias for long result
typedef WPARAM WParam;                              // Alias for word parameter
typedef LPARAM LParam;                              // Alias for long parameter
typedef HINSTANCE HInstance;                        // Alias for instance handle